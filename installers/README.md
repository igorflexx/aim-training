# Installers

Здесь лежат готовые desktop-сборки проекта `AimForge`.

- `macos-arm` — установщик и `.app` для `macOS Apple Silicon`
- `windows` — папка под Windows-установщик

Версия для `macOS Intel` не создается и не поддерживается.
