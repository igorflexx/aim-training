# Windows Installer

Эта папка предназначена для Windows-установщика `AimForge`.

Если локально `.exe` еще не лежит в папке, его можно получить через workflow:

- `.github/workflows/build-windows-exe.yml`

После сборки сюда можно положить готовый `.exe` или `.msi`.
